<% provide(:title, "Edit food") %>
<h1>Edit food</h1>

<div class="row">
	<div class="span6 offset3">
		<%= form_for(@food) do |f| %>
			<%= render 'fields', f: f %>
		    <%= f.submit "Save changes", class: "btn btn-primary" %>
		<% end %>
	</div>
</div>