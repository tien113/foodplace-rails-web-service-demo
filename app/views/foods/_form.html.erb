<%= form_for(@food) do |f| %>
  <% if @food.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@food.errors.count, "error") %> prohibited this food from being saved:</h2>
      <ul>
      <% @food.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    Name<br />
    <%= f.text_field :food_name %>
  </div>
  <div class="field">
    Price<br />
    <%= f.text_field :food_price %>
  </div>
  <div class="field">
    Image Url<br />
    <%= f.text_field :food_image_url %>
  </div>
  <div class="field">
    Ingredient<br />
    <%= f.text_area :food_ingredient %>
  </div>
  <div class="field">
   Place<br />
	<%= collection_select(:food, :place_id, @places, :id, :place_name) %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
