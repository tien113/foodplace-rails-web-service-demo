<% if @food.errors.any? %>
	<div id="error_explanation">
		<div class="alert alert-error">
			The form contains <%= pluralize(@food.errors.count, "error") %>.
		</div>
			<ul>
				<% @food.errors.full_messages.each do |msg| %>
					<li>* <%= msg %></li>
				<% end %>
			</ul>
	</div>
<% end %>